apiVersion: v1
kind: Service
metadata:
  name: database # Sets service name
  labels:
    app: database # Labels and Selectors
spec:
  type: NodePort # Sets service type
  ports:
    - port: 5432 # Sets port to run the postgres application
  selector:
    app: database
